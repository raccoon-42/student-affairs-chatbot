[pytest]
markers =
    integration: needs running services (API server, Qdrant, LLM). Run with: pytest -m integration
addopts = -m "not integration"
# test ids carry Turkish text (judge case descriptions); render it as-is
# instead of ı-style escapes
disable_test_id_escaping_and_forfeit_all_rights_to_community_support = True
